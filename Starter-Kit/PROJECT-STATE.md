# Project State / Estado del Proyecto

> Living document — update it whenever something meaningful changes.
> Documento vivo — actualízalo cuando cambie algo relevante.

**Last updated / Última actualización:** YYYY-MM-DD

## ES
- **Qué es:** portal de capacitación bilingüe (EN/ES, inglés por defecto), por áreas, en GitHub Pages.
- **Áreas actuales:** (edítalas en `index.html` → `AREAS`) …
- **Módulos actuales:** (uno por línea: área · título · archivo) …
- **Convenciones:** ver `WORKING-RULES.md`. Motor de módulos en `animations/module-template.html`; portadas SVG en `assets/` (generador en `tools/poster-generator.html`); fotos JPG en `assets/` (`../assets/…`).
- **Probar:** `python -m http.server 8000` dentro de la carpeta; abrir `http://localhost:8000`.
- **Pendientes / ideas:** …

## EN
- **What it is:** bilingual training portal (EN/ES, English default), by areas, on GitHub Pages.
- **Current areas:** (edit them in `index.html` → `AREAS`) …
- **Current modules:** (one per line: area · title · file) …
- **Conventions:** see `WORKING-RULES.md`. Module engine in `animations/module-template.html`; SVG posters in `assets/` (generator in `tools/poster-generator.html`); JPG photos in `assets/` (`../assets/…`).
- **Preview:** `python -m http.server 8000` inside the folder; open `http://localhost:8000`.
- **To do / ideas:** …
